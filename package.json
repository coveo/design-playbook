{
  "name": "design-playbook-root",
  "private": true,
  "packageManager": "pnpm@11.22.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter design-playbook dev",
    "build": "pnpm --filter design-playbook build",
    "preview": "pnpm --filter design-playbook preview",
    "type-check": "pnpm --filter design-playbook type-check",
    "build:plays": "pnpm --filter design-playbook build:plays"
  }
}
